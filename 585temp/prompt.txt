I added a new eviction policy abstraction to Mooncake, and the internal runtime plumbing seems to exist, but the `mooncake_master` CLI still does not recognize my new flag:

    --eviction_policy=attention_aware

I need you to fix this end-to-end.

What already exists:
- `EvictionPolicyType` exists in `eviction_policy.h`
- `ParseEvictionPolicyType(...)` exists in `eviction_policy.cpp`
- `CreateEvictionPolicy(...)` exists
- `MasterService` now consumes `config.eviction_policy_type`
- `MasterService` constructs `eviction_policy_` using `CreateEvictionPolicy(config.eviction_policy_type)`
- Startup logs in `master_service.cpp` already log the selected policy once config is populated

What is missing:
- Passing `--eviction_policy=attention_aware` to `mooncake_master` does not work
- So the CLI/config parsing layer is probably not wired up yet

Useful evidence from current startup logs:
- The binary already prints a long startup summary from `master.cpp`, including parsed fields like:
  - rpc_port
  - enable_http_metadata_server
  - http_metadata_server_port
  - eviction_ratio
  - eviction_high_watermark_ratio
  - etc.
- Example log lines:
  - `master.cpp:819] Master service started on port 50051, ... eviction_ratio=0.05, eviction_high_watermark_ratio=0.95, ...`
  - `master.cpp:739] Starting C++ HTTP metadata server on 0.0.0.0:8080`
- But there is no `eviction_policy=...` field in that startup log
- This strongly suggests the CLI/config layer in `master.cpp` was never updated to parse and print the new option

What I need you to do:
1. Find where `mooncake_master` parses command-line flags / argv
2. Find where `MasterServiceConfig` is constructed/populated
3. Add support for a new CLI/config flag:
   - `--eviction_policy=<value>`
4. Parse the string with `ParseEvictionPolicyType(...)`
5. Store the result in `MasterServiceConfig::eviction_policy_type`
6. Make sure invalid values produce a reasonable error
7. Update startup logging so the chosen eviction policy is visible in the main startup summary
8. Keep changes minimal and idiomatic for this codebase

Please do this carefully:
- First identify the exact file(s) responsible for:
  - CLI flag definitions
  - config parsing
  - `MasterServiceConfig` population
  - startup summary logging
- Then explain the minimal changes needed
- Then implement them

Important constraints:
- Do not rewrite unrelated startup/config code
- Follow the project’s existing flag parsing style exactly
- If the project uses gflags, absl flags, CLI11, manual argv parsing, or another approach, preserve that style
- Keep the runtime `MasterService` policy plumbing intact unless needed
- If `MasterServiceConfig` does not already contain `eviction_policy_type`, add it in the appropriate place with a sensible default
- If it already exists, ensure the default is preserved when no flag is passed

Expected outcome:
After your change, this should work:

    mooncake_master \
      --enable_http_metadata_server=true \
      --http_metadata_server_host=0.0.0.0 \
      --http_metadata_server_port=8080 \
      --eviction_policy=attention_aware

and:
- `MasterServiceConfig::eviction_policy_type` should be set correctly
- `MasterService` should build the right policy object
- startup logs should show something like `eviction_policy=attention_aware`

Please show me:
1. which files you changed
2. the exact code changes
3. how to test the new CLI flag
4. any build/runtime caveats